---
import { type CollectionEntry, getCollection } from "astro:content";
import CardLayouts from "../../../layouts/cardLayouts.astro";

export async function getStaticPaths() {
  const cardlayouts = await getCollection("Cardlayout");
  return cardlayouts.map((card) => ({
    params: { slug: card.slug },
    props: card,
  }));
}
type Props = CollectionEntry<"Cardlayout">;

const cards = Astro.props;
const { Content } = await cards.render();
---

<CardLayouts {...cards.data}>
  <Content />
</CardLayouts>
